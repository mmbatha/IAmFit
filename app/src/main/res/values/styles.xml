<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Activity themes -->

    <style name="Theme.Base" parent="android:Theme.Light" tools:keep="@style/Theme_Base"></style>

    <style name="Theme.Sample" parent="Theme.Base" tools:keep="@style/Theme_Sample" />

    <!--<style name="AppTheme" parent="Theme.Sample" tools:keep="@style/AppTheme" />-->
    <!-- Widget styling -->

    <style name="Widget" tools:keep="@style/Widget" />

    <style name="Widget.SampleMessage" tools:keep="@style/Widget_SampleMessage">
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:lineSpacingMultiplier">1.1</item>
    </style>

    <style name="Widget.SampleMessageTile" tools:keep="@style/Widget_SampleMessageTile">
        <!--<item name="android:background">@drawable/tile</item>-->
        <item name="android:shadowColor">#7F000000</item>
        <item name="android:shadowDy">-3.5</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Log" parent="AppTheme">
        <item name="android:typeface">monospace</item>

        <item name="android:padding">@dimen/margin_medium</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:lineSpacingMultiplier">1.1</item>
    </style>

</resources>
